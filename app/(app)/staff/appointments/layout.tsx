import type { ReactNode } from "react";
import "@/app/staff/appointments/staff-appointments.css";

export default function StaffAppointmentsLayout({
  children,
}: {
  children: ReactNode;
}) {
  return children;
}
